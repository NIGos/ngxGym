# The sequence that produced four of the defects this repository exists to catch.
# Every line here replaces a minute of playing a real game to a particular state.
frames 300
mode borderless
frames 300
mode exclusive
frames 300
mode borderless
frames 300
mode windowed
frames 300

# expect-after-d3d11: the game created its feature again :: rebuilding: render
# expect-after-vk: ReShade created an effect runtime :: building the D3D12 feature
