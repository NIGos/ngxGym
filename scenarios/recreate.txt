# The game replacing its own feature. Twice at a NEW shape, once at an unchanged
# one -- the last is what Red Dead Redemption 2 does on an HDR or fullscreen toggle,
# and the case the mirror carried its stale feature across until 1.4.0.
frames 300
preset 1
frames 300
preset 2
frames 300
recreate
frames 300

# expect-after-d3d11: the game created its feature again :: rebuilding: render
# The fourth create is the unchanged-shape one (start, preset 1, preset 2,
# recreate), so the build after it is the build that step caused.
# expect-after-vk: Vulkan CreateFeature #4 returned :: building the D3D12 feature
