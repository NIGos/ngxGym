# Switching DLSS off in the game's own settings and back on.
#
# What this actually reaches, and the header used to claim more: the source latch
# is RELEASED, and the game's next evaluate takes the mirror straight back. The
# synthetic contract cannot arm in this run folder at all -- the log says
# "motion vectors: NOT PRESENT" and "not viable as things stand", because there
# is no ReShade motion-vector shader here. Claiming the handover would be a
# scenario describing a state it never enters.
frames 600
dlss off
frames 900
dlss on
frames 600

# The substitute contract is off by default as of 1.4.0 -- it is a real DLSS
# feature fed approximated inputs and it shows -- and the source-latch release
# exists to hand the session to it, so without the opt-in there is nothing to
# release to and the release does not happen. This scenario is about the
# release, so it asks for the key.
# cfg: synth=1

# expect: The source latch is released
