# What a game leaves out. Each of these has a fallback in the bridge, and a fallback
# nobody exercises is a fallback nobody knows works.
#
# omit flags is the one that matters most: with no DLSS.Feature.Create.Flags from the
# game the bridge resolves 107, which carries IsHDR and AutoExposure. That is the
# mechanism behind the Assassin's Creed Odyssey HDR overexposure report, and this is
# where it can be watched happening.
#
# 1900-frame segments so the every-1800 dump lands inside each one -- see exposure.txt.
omit flags
frames 1900
omit mvscale
frames 1900
omit jitter
frames 1900
omit none
frames 1900


omit quality
frames 1900

# expect-d3d11: the game sets no MV.Scale.X
# expect-d3d11: the game sets no Jitter.Offset.X
# expect-vk: so the mirrored feature is created with zero
# NGX on Vulkan refuses an evaluate with no Jitter.Offset (0xBAD00005, every
# frame of that segment) where NGX on D3D11 accepts one. That is the DLL under
# test, not this host, and the segment is here for the bridge's report of it.
# host-may-fail-vk
