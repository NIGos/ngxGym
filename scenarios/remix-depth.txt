# Depth handed to NGX as an R32_SFLOAT colour image, the way RTX Remix does
# it (dlss5-bridge pull request #21). The mirror used to accept only true
# depth formats and refused with "the mirror does nothing"; it has to build and
# deliver here exactly as it does for D32_SFLOAT.
frames 120
depthcolor on
frames 900

# vk-only

# nofast
# Delivery after the switch, not only a rebuild: the count line every 600
# delivered frames has to appear after the create the switch caused.
# expect-after: the game replaced its own feature :: frames delivered so far
